# Prefer the LuCI Samba4 stack. Do not enable autosamba together with samba4;
# some feeds install the same /etc/hotplug.d/block/20-smb path from both.

CONFIG_PACKAGE_luci-app-samba4=y
# CONFIG_PACKAGE_autosamba is not set
