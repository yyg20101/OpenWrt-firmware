# LuCI web collection selector for source branches without upstream LuCI defaults.
#
# Official LEDE and ImmortalWrt release branches already include LuCI through
# their source DEFAULT_PACKAGES. Fork or master-like branches that do not enable
# that upstream default use this fragment as an explicit compatibility guard.
CONFIG_PACKAGE_luci=y
