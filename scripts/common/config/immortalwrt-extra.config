# ImmortalWrt-specific config additions.
#
# This fragment is intentionally empty today: the historical ImmortalWrt x86
# application baseline now lives in shared fragments so required plugins remain
# identical across enabled full-feature profiles. Keep the fragment wired in
# devices/profiles.yml as the source-specific extension point for future
# ImmortalWrt-only options.
